// Package domain 持有领域实体与不变量。
//
// R1 阶段说明:
//   - 本包仅放置 core/ 端口签名所需的最小占位结构。
//   - 真正的实体(状态机、不变量、转换表)在 R7 落地。
//   - 占位字段不得被业务代码消费;R2-R6 期间允许通过转换层从 model/ 注入。
//
// 约束(R7 起强校验):
//   - 仅 import 标准库
//   - 不持有 GORM tag
package domain
